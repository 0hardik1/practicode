# Asset Notes

These SVG files are browser-friendly stand-ins so you can inspect image-like assets directly from the UI.

- `source-images/` contains mock source artwork.
- `expected-images/` contains the intended transformed versions.
- The live challenge service still generates and validates images independently.

