# Warmup Notes

This problem does not call any external services.

- Read `TEST_INPUT` from the environment.
- Print the answer as a JSON array.
- Use this problem to verify the run/submit flow and inspect test output.

